这是一个为你整理好的 Markdown 格式文件内容，你可以直接将其复制并保存为 `README.md` 文件。

````markdown
# 高校邦 - 计算机网络 (张晓明) 测验自动答题工具箱

这是一个针对高校邦学习平台课程测验部分的辅助工具。它包含一套前端脚本，能够实现**自动提取正确答案**、**题库自我积累/进化**以及**自动填充答案**的功能。

> **提示**：本工具箱预置了《计算机网络》课程的数据，如果您需要完成其他课程，亦可使用下文中的“提取脚本”来丰富和建立新的答案库。

## 🚀 使用流程

本工具箱包含两个脚本，配合使用可实现题库的自动滚雪球式增长：

### 1. 答题阶段 (使用脚本 B)
1.  进入测验页面。
2.  按 `F12` 打开开发者工具，切换到控制台 (Console)。
3.  运行 **脚本 B**。
4.  脚本会自动根据已有历史数据填充正确答案。
5.  *(如果没有答案或答案不全，请手动做完或留空)* -> 点击提交。

### 2. 收割阶段 (使用脚本 A)
1.  进入提交后的**结果详情页** (即能看到正确答案解析的页面)。
2.  按 `F12` 打开开发者工具，切换到控制台 (Console)。
3.  运行 **脚本 A**。
4.  脚本会自动抓取当前页面的新答案，与历史题库合并。
5.  **关键步骤**：控制台会输出生成 **新的脚本 B1** 和 **新的脚本 A1**。
6.  **请复制保存这两个新生成的脚本**，用于下一次测验 (它们包含了最新的题库)。

---

## 🛠️ 脚本 A：答案提取与进化 (Evolution Script)

* **适用场景**：测验提交后的结果详情页。
* **功能**：提取当前页面的正确答案，合并历史数据，生成下一代脚本。

```javascript
(function() {
    // ============================================================
    // 1. 历史遗产数据 (Context:91631 计算机网络)
    // ============================================================
    var historyData = {
      "91631": {
        "470021": [[0],[1],[0],[2],[2],[1,3,4],[0],[0],[3]],
        "470022": [[2],[3],[3],[1,2,3],[2],[3],[0],[1],[0]],
        "470023": [[0],[2],[3],[1],[1],[1],[3],[2],[2]],
        "470024": [[3],[0],[1],[2],[3],[3],[3],[2],[3]],
        "470025": [[1],[0],[1],[1],[0],[1],[2],[2],[1],[2]],
        "470026": [[1],[1],[0],[0],[1],[3],[3],[2],[0],[3],[1],[2],[3],[2]],
        "470027": [[0],[1],[1],[0],[2],[3],[3],[2],[3],[3]],
        "470028": [[0],[1],[1],[2],[1],[3],[2],[3],[0],[2],[3],[0]]
      }
    };

    // ============================================================
    // 2. 核心逻辑：提取当前页面新答案
    // ============================================================
    try {
        if (typeof questionList === 'undefined' || typeof quizInfo === 'undefined') {
            throw new Error("环境错误：未找到 questionList。请确保你在【提交后的结果页】运行此脚本。");
        }

        var currentCid = quizInfo.contextId;
        var currentQid = quizInfo.quizId;
        var currentAns = [];

        // 遍历提取 correct="1" 的选项索引
        questionList.forEach(function(q) {
            var idxs = [];
            q.answerList.forEach(function(a, idx) {
                if (a.correct == "1") idxs.push(idx);
            });
            currentAns.push(idxs);
        });

        console.log(`[系统] 成功提取当前测验答案 (Context:${currentCid}, Quiz:${currentQid})`);

        // ============================================================
        // 3. 数据合并 (进化过程)
        // ============================================================
        if (!historyData[currentCid]) historyData[currentCid] = {};
        historyData[currentCid][currentQid] = currentAns;

        var newDataJson = JSON.stringify(historyData, null, 2);

        // ============================================================
        // 4. 生成下一代脚本 (构造器)
        // ============================================================
        
        // --- 生成脚本 B1 (填充者) ---
        var scriptB1 = `
/* [脚本 B1] 答案自动填充 (包含 ${Object.keys(historyData).length} 个课程数据) */
/* 使用方法：在测验答题页面控制台运行 */
var data = ${newDataJson};
var answers = data[quizInfo.contextId] ? data[quizInfo.contextId][quizInfo.quizId] : null;
if (answers) {
    $('.question-item').each(function (i) {
        var boxes = $(this).find('[answer_id]');
        if (answers[i]) {
            answers[i].forEach(function (x) { boxes.eq(x).click(); });
        }
    });
    console.log("✅ 答案已根据历史数据库自动填充！");
} else {
    console.warn("❌ 数据库中暂无此测验(ID:" + quizInfo.quizId + ")的答案。");
}
`;

        // --- 生成脚本 A1 (下一代提取者) ---
        var scriptA1Logic = arguments.callee.toString();
        var scriptA1 = scriptA1Logic.replace(
            /var historyData = \{[\s\S]*?\};/, 
            `var historyData = ${newDataJson};`
        );
        scriptA1 = "(" + scriptA1 + ")();";

        // ============================================================
        // 5. 输出结果
        // ============================================================
        console.clear();
        console.group("%c🎉 脚本生成完毕！", "font-size: 16px; color: green;");
        
        console.log("%c👇 1. 脚本 B1 (请复制保存，用于填答案) 👇", "font-weight:bold; color: #d35400");
        console.log(scriptB1);
        
        console.log("%c👇 2. 脚本 A1 (请复制保存，用于下次提交后提取新数据) 👇", "font-weight:bold; color: #2980b9");
        console.log(scriptA1);
        
        console.groupEnd();

    } catch (e) {
        console.error("运行失败: " + e.message);
    }
})();
````

-----

## 📝 脚本 B：自动填充 (Auto-Fill Script)

  * **适用场景**：测验答题页面 (提交前)。
  * **功能**：根据内置的数据库，自动勾选正确选项。

*(注：如果你运行过脚本 A，请优先使用脚本 A 生成的新版 B1 脚本，以下是截至目前的最新预设版本，适用于计算机网络课程)*

```javascript
/* [脚本 B] 答案自动填充 */
/* 使用方法：在测验答题页面控制台运行 (F12 -> Console -> 粘贴 -> 回车) */
var data = {
  "91631": {
    "470021": [[0],[1],[0],[2],[2],[1,3,4],[0],[0],[3]],
    "470022": [[2],[3],[3],[1,2,3],[2],[3],[0],[1],[0]],
    "470023": [[0],[2],[3],[1],[1],[1],[3],[2],[2]],
    "470024": [[3],[0],[1],[2],[3],[3],[3],[2],[3]],
    "470025": [[1],[0],[1],[1],[0],[1],[2],[2],[1],[2]],
    "470026": [[1],[1],[0],[0],[1],[3],[3],[2],[0],[3],[1],[2],[3],[2]],
    "470027": [[0],[1],[1],[0],[2],[3],[3],[2],[3],[3]],
    "470028": [[0],[1],[1],[2],[1],[3],[2],[3],[0],[2],[3],[0]]
  }
};

var answers = data[quizInfo.contextId] ? data[quizInfo.contextId][quizInfo.quizId] : null;
if (answers) {
    $('.question-item').each(function (i) {
        var boxes = $(this).find('[answer_id]');
        if (answers[i]) {
            answers[i].forEach(function (x) { boxes.eq(x).click(); });
        }
    });
    console.log("✅ 答案已根据历史数据库自动填充！");
} else {
    console.warn("❌ 数据库中暂无此测验(ID:" + quizInfo.quizId + ")的答案。请先提交一次(可空卷)并运行脚本A以提取答案。");
}
```

-----

## ⚠️ 免责声明

本仓库脚本仅供技术交流与学习使用，请勿用于违反学术诚信的行为。开发者不对使用本脚本产生的任何后果负责。

```
```
